# NuPai当前P0P1P2待办全景

> 类型：故障复盘 | 项目：_global | 风险：L4 | 状态：草稿
> 同步自 Feishu Bitable 表 A | 2026-05-02T11:00:00Z

## 摘要

处置顺序：P0 立刻 → P1 本月内 → P2 季度内。每条都有独立卡讲细节和修法。本卡是入口型汇总卡，每周盘点一次，关一条划掉一条。

## 正文

> 记录日期：2026-04-17  风险等级：L4

## 背景
2026-04-17 整理基础设施清单 v1.0 时审计两台服务器，发现 8 条未解决的运维/安全/灾备问题，按 P0/P1/P2 分类汇总到基础设施清单 §15。需要一卡看清"还有哪些坑没填"，便于周会和审计盘点。

## 内容
🔴 P0（必须立即处置）：CRM 无数据库备份 → 数据丢失无法恢复（细节见"CRM无数据库备份P0风险"卡）CRM Skyvern 9222 端口（Chrome DevTools 远程调试）对公网开放 → 安全漏洞（细节见"NuPai公网暴露高危端口"卡）Store Docker daemon 连不上 Docker Hub（STO-203）→ 阻塞所有 docker build / deploy（细节见"STO-203镜像加速器是部署前提"卡）🟠 P1（重要待办）： 4. Store Grafana(3000) 和 Prometheus(9090) 对公网开放 → 信息泄露（细节见"Store监控容器3个清单"+"Store端口映射全表"卡） 5. CRM nginx server_name 是 localhost → 应配置正确域名（细节见"Store/CRM域名SSL现状"卡） 6. Dify 数据库无独立备份 → Dify 数据丢失无法恢复（细节见"Dify附属PostgreSQL数据库"卡） 7. Store Dockerfile.prod 无阿里云 apt + pypi 源（STO-202）→ Docker Hub 恢复后 apt 层仍有超时隐患（细节见"STO-202 Dockerfile阿里云源"卡）🟡 P2（安全基线提升）： 8. Store 防火墙无额外规则（iptables INPUT ACCEPT）→ 安全基线不够（细节见"Store/CRM防火墙规则"卡）

## 结论
处置顺序：P0 立刻 → P1 本月内 → P2 季度内。每条都有独立卡讲细节和修法。本卡是入口型汇总卡，每周盘点一次，关一条划掉一条。

## 影响
基础设施清单 §15 的更新节奏、运维周会议题、安全审计报告、灾备能力评分、部署稳定性、CR/部署的风险检查项、Linear 票根因列表。

## 下次注意
每周固定扫一遍本卡，关一条更新一条；新发现的问题立即追加 §15 + 同步本卡；P0 不允许跨月仍挂着；新增问题按同样 P0/P1/P2 模板分级；问题修复后保留历史在变更日志（不要直接删，以免再犯）。


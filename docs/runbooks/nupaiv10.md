# NuPai基础设施清单v1.0

> 类型：操作规程 | 项目：_global | 风险：L3 | 状态：草稿
> 同步自 Feishu Bitable 表 A | 2026-05-02T11:06:02Z

## 摘要

本文档是 NuPai 运维入口。运维前先查 4 张速查表再下手。新增容器/端口/数据库/域名/环境变量必须同步更新本文档，CI 检查若没拦下变更要补强 hook。

## 正文

> 记录日期：2026-04-17  风险等级：L3

## 背景
NuPai 基础设施信息原本分散在各仓库 CLAUDE.md、Claude 记忆、各服务器现场，每次运维都现查易出错。2026-04-17 整理出 v1.0 完整文档统一管理，并建立“基础设施变更必须同步本文档（CI 强制）”规则。

## 内容
v1.0 文档结构（18 节 + 4 个速查表 + 变更日志 + 待办 + 命名规范）：速查表：① 容器怎么更新 ② 服务器在哪 ③ 数据库 ④ compose 命令格式。正文 §1 服务器清单（3 台）→ §2 容器清单（Store 21 + CRM 9）→ §3 docker-compose 文件关系 → §4 容器更新方式（本地挂载 vs 镜像打包）→ §5 端口映射总表 → §6 环境变量 Key 清单 → §7 数据库 → §8 备份策略 → §9 域名与 SSL → §10 防火墙 → §11 定时任务 → §12 开发环境 Mac Mini → §13 第三方服务依赖 → §14 变更日志 → §15 待办（含 P0/P1）→ §16 命名规范 → §17 运维监控 → §18 部署前提与镜像策略（STO-202/203/204 根因回写）。更新规则写死：“任何基础设施变更（容器/端口/环境变量/数据库/域名）必须同步更新本文档（CI 强制检查）”。配套文档：docs/OPS-COMMANDS.md（标准运维命令白名单）。

## 结论
本文档是 NuPai 运维入口。运维前先查 4 张速查表再下手。新增容器/端口/数据库/域名/环境变量必须同步更新本文档，CI 检查若没拦下变更要补强 hook。

## 影响
所有运维 SOP、deploy.sh、新人 onboarding、故障排查、安全审计、新增资源审批流；OPS-COMMANDS.md 配套使用；CI hook（enforce 类）依赖本文档作为基线。

## 下次注意
新增容器/端口/数据库/域名/环境变量后必须同步更新本文档；CI 检查若未触发要补强 hook；运维前先查 4 张速查表（容器/服务器/数据库/compose 命令）；本卡 + 32 张细分卡共同覆盖 v1.0 全部细节，找具体信息走细分卡，找全貌看本文档。

